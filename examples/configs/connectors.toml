# Free-core config with live source connectors and file-backed memory.
# Synthesis and Telegram remain optional (see examples/radar.toml).

title = "AI Research Radar"
topic = "agentic AI systems"
watch_terms = ["agents", "memory", "evaluation", "MLOps"]
output_path = "briefs/today.md"
max_items = 5
sources = ["arxiv", "hackernews", "github", "huggingface"]
connector_timeout_seconds = 10
memory_path = ".radar/memory.json"
