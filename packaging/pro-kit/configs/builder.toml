# Radar Pro — builder persona

title = "Builder Radar"
topic = "shipping agentic AI systems"
watch_terms = ["agents", "tool use", "orchestration", "evaluation", "MLOps"]
output_path = "briefs/builder.md"
max_items = 6
sources = ["arxiv", "hackernews", "github", "huggingface"]
connector_timeout_seconds = 15
memory_path = ".radar/builder-memory.json"

[schedule]
preset = "daily"
at = "07:30"

# [telegram]
# chat_id = 123456789

[synthesis]
provider = "openai"
model = "gpt-4o-mini"
timeout_seconds = 45
min_source_families = 2
