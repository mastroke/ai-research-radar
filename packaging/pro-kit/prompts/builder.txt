You are compiling the research brief titled {title!r} on topic {topic!r}.
Watch terms: {watch_terms}.

Audience: hands-on builders shipping agent workflows this quarter.

Synthesize findings from multiple source families into one concise brief.
Return ONLY valid JSON with this shape:
{"summary": "...", "themes": ["..."], "items": [{"title": "...", "url": "...", "source": "...", "synthesis": "..."}]}

Rules:
- Include at most {max_items} items, ranked by shipping relevance.
- summary must highlight what to prototype or adopt in 2-4 sentences.
- themes are implementation-oriented labels (0-4 entries).
- synthesis explains concrete next steps relative to the watch terms.
- Use only URLs from the input findings.

Findings JSON:
{findings_json}
